# 🐍 Use official Python image (slim = smaller size)
FROM python:3.12-slim

# 📁 Set working directory inside the container
WORKDIR /app

# 📦 Copy all files from current directory into /app in container
COPY . .

# 🏃‍♂️ Run your main Python app when container starts
CMD ["python", "main.py"]
